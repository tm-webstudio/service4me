"use client"

import { ProtectedRoute, useAuth } from '@/lib/auth-v2'
import { Navigation } from '@/components/navigation'
import { Footer } from '@/components/footer'

function MultiRoleContent() {
  const { user, signOut } = useAuth()

  return (
    <div className="min-h-screen bg-gray-50">
      <Navigation />
      <div className="max-w-4xl mx-auto px-6 py-12">
        <div className="bg-white rounded-lg shadow-sm border p-8">
          <div className="mb-6">
            <span className="inline-flex items-center rounded-full bg-blue-50 px-3 py-1 text-sm font-medium text-blue-700">
              Multi-Role Access
            </span>
          </div>

          <h1 className="text-3xl font-bold text-gray-900 mb-4">
            Multi-Role Test Page
          </h1>

          <p className="text-gray-600 mb-8">
            This page allows access to both clients AND stylists (but not admins).
          </p>

          <div className="bg-gray-50 rounded-lg p-6 mb-8">
            <h2 className="text-lg font-semibold text-gray-900 mb-4">Your Profile</h2>
            <dl className="space-y-2">
              <div>
                <dt className="text-sm font-medium text-gray-500">Email</dt>
                <dd className="text-sm text-gray-900">{user?.email}</dd>
              </div>
              <div>
                <dt className="text-sm font-medium text-gray-500">Full Name</dt>
                <dd className="text-sm text-gray-900">{user?.fullName || 'Not set'}</dd>
              </div>
              <div>
                <dt className="text-sm font-medium text-gray-500">Role</dt>
                <dd className="text-sm text-gray-900 font-semibold">{user?.role}</dd>
              </div>
              <div>
                <dt className="text-sm font-medium text-gray-500">User ID</dt>
                <dd className="text-sm text-gray-900 font-mono text-xs">{user?.id}</dd>
              </div>
            </dl>
          </div>

          <div className="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-8">
            <h3 className="text-sm font-semibold text-blue-900 mb-2">
              Route Protection Test - Multiple Roles
            </h3>
            <p className="text-sm text-blue-800 mb-3">
              This page demonstrates how to allow multiple roles using allowedRoles prop.
            </p>
            <div className="bg-white rounded p-3 font-mono text-xs">
              <code>
                {'<ProtectedRoute allowedRoles={[\'client\', \'stylist\']}>'}
              </code>
            </div>
          </div>

          <div className="grid grid-cols-2 gap-4 mb-8">
            <div className="bg-green-50 rounded-lg p-4">
              <h4 className="font-semibold text-green-900 text-sm mb-1">✓ Clients</h4>
              <p className="text-xs text-green-800">Can access this page</p>
            </div>
            <div className="bg-green-50 rounded-lg p-4">
              <h4 className="font-semibold text-green-900 text-sm mb-1">✓ Stylists</h4>
              <p className="text-xs text-green-800">Can access this page</p>
            </div>
            <div className="bg-red-50 rounded-lg p-4 col-span-2">
              <h4 className="font-semibold text-red-900 text-sm mb-1">✗ Admins</h4>
              <p className="text-xs text-red-800">Cannot access this page (will be redirected)</p>
            </div>
          </div>

          <div className="flex gap-4">
            <button
              onClick={() => signOut()}
              className="inline-flex justify-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 shadow-sm hover:bg-gray-50"
            >
              Sign Out
            </button>
          </div>
        </div>
      </div>
      <Footer />
    </div>
  )
}

export default function TestProtectedMultiPage() {
  return (
      <ProtectedRoute allowedRoles={['client', 'stylist']}>
        <MultiRoleContent />
      </ProtectedRoute>
    </AuthProvider>
  )
}
